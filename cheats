git cheats.
fork links show all changes.
git can import many formats.
use git to alter blog data and entries.
import files into branches to help commit, files should be reordered and then compiled for application. 
